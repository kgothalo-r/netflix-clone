import styled from "styled-components";

export const Wrapper = styled.div`
    background-color: rgb(0, 0, 0,);
    color: var(--lightGrey);
    font-size: 4em;
    text-align: center;
    margin-top: 3em;
    opacity: 0.4;
    line-height: 0;
`;